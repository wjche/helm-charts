#Fluentd Setup for Local Deployment
Reuse the fluentd chart in [helm/charts/stable/fluentd](https://github.com/helm/charts/tree/master/stable/fluentd)

## Run Command
helm install stable/fluentd --name fluentd -f values.yml

